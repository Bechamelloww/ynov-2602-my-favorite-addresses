info:
  name: Create token
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3000/api/users/tokens/
  body:
    type: json
    data: |-
      {
        "email": "67@67.67",
        "password": "67"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
