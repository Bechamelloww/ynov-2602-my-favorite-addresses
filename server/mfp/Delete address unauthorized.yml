info:
  name: Delete address unauthorized
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseURL}}/addresses/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

