info:
  name: Create user
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/users/"
  body:
    type: json
    data: |-
      {
        "email": "67@67.67",
        "password": "67"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
