name: MFP CI Backend Tests
run-name: ${{ github.actor }} is running the tests for his last commit 🚀
on: [push]
jobs:
  backend-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v5
      - name: Run tests
        run: |
          cd server
          npm install
          npm run test
